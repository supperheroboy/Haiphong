<!-- 

-- các bước đẩy lên git khi code xong
    // git pull: lấy code mới nhất


 B1- git status: // kiểm tra trạng thái thay đổi của files

 B2 - git add . : // add các files thay đổi

 B3 - git commit -m " message " : // note lại cụ thể làm gì

 B4 - git push origin <tên nhánh> : // đẩy code lên cloud 

 -->

 <!-- các câu lệnh hay dùng
    git branch: kiểm tra branch
    git checkout <tên branch> : checkout sang branch khác
    git checkout -b <tên branch> : tạo mới và checkout sang branch mới đó
  -->